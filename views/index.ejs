<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Notes App</title>
  </head>
  <body>

    <div class="main w-full min-h-screen bg-zinc-900 ">
      <div class="form p-10 text-white">
        <h3 class="text-5xl py-8">Create Notes</h3>
        <form action="/create" method="post">
          <input
            class="block w-full rounded-md outline-none px-3 py-2 bg-zinc-800 border"
            type="text"
            placeholder="Title goes here"
            name="title"
          />
          <textarea
            class="block w-full rounded-md outline-none mt-2 px-3 py-2 resize-none bg-zinc-800 border"
            placeholder="Write your task details here..."
            name="details"
          ></textarea>
          <input
            class="mt-2 bg-blue-600 px-5 py-2 rounded-lg border "
            type="submit"
            value="Crete Task"
          />
        </form>
      </div>
      <div class="tasks p-10 flex gap-3 flex-wrap">
        <%  if(files.length>0){ %>
        <% files.forEach(function(val){ %>
            <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
                <h1 class="text-white text-2xl tracking-tighter">
                    <%= val %>
                </h1>
                <div class="flex w-full justify-between mt-3 items-center pt-6">
                    <a class="text-white inline-block text-md border border-white px-2 bg-blue-600 rounded-lg " href="/file/<%= val %>">Open</a>
                    <div class="flex justify-between gap-5">
                      <a class="text-white inline-block text-md border border-white px-2 bg-green-600 rounded-lg " href="/edit/<%= val %>">edit</a>
                      <a class="text-white inline-block text-md border border-white px-2 bg-red-600 rounded-lg " href="/delete/<%= val %>">delete</a>
                    </div>
                </div>
            </div>
       <% })%>
        
       <% }else{  %>
            <h3 class="text-zinc-600 ml-3">No Task Yet</h3>
       <% } %>
      </div>
    </div>
  </body>
</html>
